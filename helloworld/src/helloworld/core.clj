(ns helloworld.core);this is namespace decleration,
;every clojure code should have namespace so other clojure codes can require it.


(defn -main
  [];this is a vector which declares what arguments this function requires
  (println "HelloWorld")
  )
