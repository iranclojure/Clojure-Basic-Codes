(ns functions.core)

(defn foo
  "sample function"
  [];function arguments ;
  ;from here everything is called function body
  ;last argument will return as function result
  true;anything as last argument will return
  )
(defn -main [] (println (foo)))
